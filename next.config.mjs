/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: '/my-nextjs-app',
    assetPrefix: '/my-nextjs-app',
    trailingSlash: true,
};

export default nextConfig;
